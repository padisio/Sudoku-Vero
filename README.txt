# Sudoku PWA — clean repo

Sube estos 5 ficheros y la carpeta /icons a la raíz de un repo y despliega en Vercel como Static/Other.

Comprobación:
- /manifest.webmanifest → 200 OK
- /service-worker.js → 200 OK
- DevTools → Application → Manifest/Service Workers: ambos detectados.
